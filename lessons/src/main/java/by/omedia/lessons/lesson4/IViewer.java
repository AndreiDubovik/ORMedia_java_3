package by.omedia.lessons.lesson4;

public interface IViewer {
	/*
	 * классов, раилизующих интерфейс может быть больше одного
	 * но любой из них должен каким-то образом показать объект
	 */
	void show(Object ob);
}
